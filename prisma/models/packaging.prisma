model Packaging {

    id              Int @id @default(autoincrement())
    name            String
    description     String
    image           String
    
}

// Tem alguma relação com a tabela de produtos mas não está clara ainda